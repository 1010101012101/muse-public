
There are separate scripts created to help run this extractor in parallel as it can a long time run over the entire corpus.

Run each of the scripts named: extract_files_**.sh in seprate tmux sessions.  each takes a single paramater to denote corpus directory
