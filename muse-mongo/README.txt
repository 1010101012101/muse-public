tmux-new.sh will start a new tmux session named mongo
tmux-attach.sh will attach to a tmux session named mongo

The command to start the mongo server in the tmux session is:
> sudo docker-compose up -d


