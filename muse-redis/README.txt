tmux-new.sh will start a new tmux session named redis
tmux-attach.sh will attach to a tmux session named redis

The command to start the redis server in the tmux session is:
> sudo docker-compose up -d


